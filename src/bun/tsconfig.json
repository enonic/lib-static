{
  "compilerOptions": {
    // "moduleResolution": "node",
    "paths": {
      // Doesn't work for bun?
      // "/lib/enonic/asset/*": ["../main/resources/lib/enonic/asset/*"],
      // "/lib/enonic/static/*": ["../main/resources/lib/enonic/static/*"]
    },
    "strict": true,
  },
  "include": [
    "**/*.ts"
  ]
}
